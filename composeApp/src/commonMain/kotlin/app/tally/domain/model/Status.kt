package app.tally.domain.model

/** AGENTS.md §8. Pulled forward from Phase 1.1 — [app.tally.ui.components.TallyStatusBadge] needs the real type, not a UI-local stand-in. */
public enum class Status { ACTIVE, DUE, OVERDUE, PAUSED, TRIAL }
